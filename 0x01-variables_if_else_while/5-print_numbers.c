#include <stdio.h>

/**
*main - Entry point
*
*Return: Always 0
*
*programme that prints all single digits of base 10
*
*/

int main(void)
{
int a = 0;

while (a <= 9)
{
printf("%d", a);
a++;
}
putchar('\n');

return (0);
}

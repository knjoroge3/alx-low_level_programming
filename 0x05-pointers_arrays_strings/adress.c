#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always zero
 */

int main(void)
{
	char c;
	int n;

	printf("The address of 'c' is: %p\n", &c);
	printf("The address of 'n; is: %p\n", &n);

	return (0);
}
